<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field">
    <%= f.text_field :email %>
    <%= f.label :email %>
  </div>

  <div class="input-field">
  <%= f.select :role, User.roles.keys.map{|k| [k.titleize, k] }, include_blank: true %>
  <%= f.label :role %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
